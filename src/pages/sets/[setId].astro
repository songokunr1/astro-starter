---
import Layout from "../../layouts/Layout.astro";
import SetDetailPage from "../../components/feature/sets/SetDetailPage";

const { setId } = Astro.params;

if (!setId) {
  throw new Error("Missing setId parameter");
}
---

<Layout title="Set Details | Fiszki AI">
  <SetDetailPage client:load setId={setId} />
</Layout>
